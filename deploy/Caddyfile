# Reemplaza "tudominio.org" por tu dominio real (el setup.sh lo hace por ti).
# Caddy obtiene y renueva el certificado HTTPS automáticamente.
tudominio.org, www.tudominio.org {
	encode gzip
	reverse_proxy localhost:4599
}
